# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
playwright-report
*.tsbuildinfo
test-results
coverage
*.local
*.pem
.env
.env.deploy
.env.convex

# Generated sideload manifests (scripts/manifest.sh output); source is public/manifest.xml
/manifest-*.xml

# Generated agent wiki snapshot (factory-droid). Local staging only — its real home
# is droid cloud + the GitHub wiki repo, so it must never be committed here (a tracked
# copy would drift from the published wiki). Source of truth for agent rules is
# CLAUDE.md / convex/_generated/ai/guidelines.md, NOT this snapshot.
droid-wiki/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
